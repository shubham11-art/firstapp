import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#140a99",
    },
    secondary: {
      main: "#17de99",
    },
    otherColor: {
      main: "#000000",
    },
    border: {
      borderRadius: "1px solid red",
    },
  },
});
